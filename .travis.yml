addons:
  apt:
    packages:
    - libav-tools
after_success: bash <(curl -s https://codecov.io/bash)
cache:
  directories:
    - coconuts/static/node_modules
  pip: true
dist: trusty
install:
  - pip install coverage tox
  - cd coconuts/static
  - npm install
  - cd ../..
language: python
script:
  - tox
  - cd coconuts/static
  - npm test -- --single-run
  - cd ../..
