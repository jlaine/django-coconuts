language: python
python:
  - "2.7"
env:
  - DJANGO=1.6
install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install libjpeg8-dev
  - pip install -q Django==$DJANGO --use-mirrors
  - pip install PIL --use-mirrors
script: python manage.py test coconuts
